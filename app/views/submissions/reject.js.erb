$(".reject-link#<%= @submission.id %>").replaceWith("<a href='/submissions/<%= @submission.id %>/approve' data-remote='true' class='approve-link moderate-link' id='<%= @submission.id %>'>Approve</a>");