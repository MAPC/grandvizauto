<h1>Edit your profile</h1>

<%= form_for @user do |f| %>

  <%= f.label :name, 'Name' %>
  <%= f.text_field :name %>

  <%= f.label :email, 'Email' %>
  <%= f.text_field :email %>

  <%= f.submit 'Update' %>
<% end %>